# Learnings & Post-Mortems

## CLI Tooling
- **Google Antigravity CLI Binary Name:** The command is `agy` (not `agy-cli` or `agi.cli`).
  All references, process executions, health checks, and documentation must invoke `agy`.
